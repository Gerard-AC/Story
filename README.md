# Story
learning git commands


git init

git status

git add .
//git diff filename
//git checkout filename -- to rollback

git commit -m "..."

git remote add origin "..."

git push -u origin master
